<p id="notice"><%= notice %></p>

<div class="container">
  <div class="row">
    <div class="col-sm-12 col-md-8 col-lg-5 px-5 px-sm-0 mx-auto">
      <h2>Edit Book</h2>

        <%= form_with model:@post_book, local:true do |f| %>

          <div class="form-group">
            <label for ="book-title">Title</label>
            <%= f.text_field :title, autofocus: true, class: "form-control", id:"inpuTtitle", placeholder:"タイトル"%>
          </div>

          <div class="form-group">
            <label for ="book-opinion">Opinion</label>
            <%= f.text_area :Opinion, autofocus: true, class: "form-control", id:"inputOpinion"%>
          </div>

          <%= f.submit "Update Book", class: "btn btn-secondary" %>
        <% end %>
        
        <%= link_to post_book.title, book_path(current_user.id) %>
        <%= link_to post_book.title, book_path(current_user.id) %>
    </div>
  </div>
</div>
<p id="notice"><%= notice %></p>

<div class="container px-5 px-sm-0">
  <div class="row">
      <!--部分テンプレートの呼び出し↓-->
      <%= render 'books/userinfo', post_book: @post_book, user: @user %>


    <div class="col-lg-8 offset-md-1">
      <h2>Book derail</h2>

        <table class="table table-striped">
          <tbody>
              <tr>
                <td>git
                    <%= link_to user_path(@user.id) do %>
                      <%= attachment_image_tag @post_books.user, :profile_image, :fill, 100, 100, format: 'jpeg', fallback: "no_image.jpg", size:'40x40' %>
                    <% end %>
                    <br>
                    <%= link_to @user.name, user_path(@user.id) %>
                  </a>
                </td>
                <td><%= link_to @post_books.title, book_path(@post_books.id) %></td>
                <td><%= @post_books.opinion %></td>
                <td><%= link_to 'Edit', edit_book_path, class: "btn btn-sm btn-success" %></td>
                <td><%= link_to 'Destroy', book_path(@post_books.id), method: :delete, data: { confirm: '本当に消去しますか?' }, class: "btn btn-sm btn-danger" %></td>
              </tr>
          </tbody>
        </table>
    </div>
  </div>
<br>

